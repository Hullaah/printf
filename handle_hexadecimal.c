#include <format_specifier.h>
#include <handlers.h>
#include <stdarg.h>
#include <utils.h>

void handle_hexadecimal(struct FormatSpecifier *fs, va_list *args, char *buf,
			int *len)
{
	UNUSED(fs);
	UNUSED(args);
	UNUSED(buf);
	UNUSED(len);
}
